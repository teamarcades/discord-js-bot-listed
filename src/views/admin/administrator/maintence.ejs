<%- include('../static/head.ejs', { bot,user,path})  %>
<div id="content" class="main-content">
    <br><br><br><br><br><br>
    <div class="container">

<div class="row">
    <div class="col-lg-6 col-md-8 col-sm-6 col-12 layout-spacing">
        <p>Modo de mantenimiento</p>
        <form action="/admin/maintence" method="POST">
          <input type="hidden" value="<%= _token %>" name="_token" id="_token">
        <div class="form-row mb-4">
            <div class="form-group col-md-4">
                <label for="inputEmail4">Razon<span style="color:red">*</span></label>
                <input type="text" class="form-control" id="reason" name="reason" placeholder="" required>
            </div>  
        </div>

      <button type="submit" name="ban" class="btn btn-primary mt-3"><i class="fa fa-server"></i> Abrir Mantenimiento</button>
    </form>
    <form action="/admin/unmaintence" method="POST">
      <input type="hidden" value="<%= _token %>" name="_token" id="_token">
        <button type="submit" name="unban" class="btn btn-danger mt-3"><i class="fa fa-server"></i> Cerrar Mantenimiento</button>
      </form>
    </div>
</div>

<br><br><br><br><br><br><br><br><br><br><br><br><br>

<script>
    var urlParams = new URLSearchParams(window.location.search);
    if(urlParams.has('success') && urlParams.get('success')){
    Swal.fire({
     icon: 'success',
     title: 'Succesfull...',
     closeOnConfirm: false,
     text: urlParams.get('message'),
     footer: 'Copyright 2023 &copy teamarcades.xyz - All rights reserved.'
    }).then((result) => {
  if (result.isConfirmed) {
    window.location.href = '/admin/maintence';
  }
})
    }
    if(urlParams.has('error') && urlParams.get('error')){
    Swal.fire({
     icon: 'error',
     title: 'Oops...',
     text: urlParams.get('message'),
     footer: 'Copyright 2023 &copy teamarcades.xyz - All right reserved.'
    }).then((result) => {
  if (result.isConfirmed) {
    window.location.href = '/admin/maintence';
  }    
})
    }
   </script>
        <%- include('../static/foot.ejs', { bot,user,path}) %>
    </div>